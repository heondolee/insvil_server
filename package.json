{
  "name": "insvil_server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate:dev": "NODE_ENV=development npx sequelize-cli db:migrate",
    "migrate:test": "NODE_ENV=test npx sequelize-cli db:migrate",
    "migrate:prod": "NODE_ENV=production npx sequelize-cli db:migrate",
    "rollback:dev": "NODE_ENV=development npx sequelize-cli db:migrate:undo",
    "rollback:test": "NODE_ENV=test npx sequelize-cli db:migrate:undo",
    "rollback:prod": "NODE_ENV=production npx sequelize-cli db:migrate:undo"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "archiver": "^7.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "exceljs": "^4.4.0",
    "express": "^4.19.2",
    "html-to-text": "^9.0.5",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.0.0",
    "sequelize": "^6.37.3",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
